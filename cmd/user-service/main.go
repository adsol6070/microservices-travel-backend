package main

import (
	// "log"
	// "microservices-travel-backend/internal/user-service/adapters/repositories"
)

func main() {
	// repo, err := repositories.NewPostgresUserRepository()

	// if err != nil {
	// 	log.Fatalf("Failed to create repository: %v", err)
	// }

	// service := services.NewUserService(repo)

	// hotelHandler := handlers.(service)

	// router := mux.NewRouter()

	// hotelHandler.RegisterRoutes(router)

	// port := ":7000"
	// log.Printf("Starting hotel-booking service on port %s...", port)
	// err = http.ListenAndServe(port, router)
	// if err != nil {
	// 	log.Fatalf("Failed to start server: %v", err)
	// }
}
